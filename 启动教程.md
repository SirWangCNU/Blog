# 项目启动教程

## 环境要求

- Node.js 22.13.0 或更高版本
- pnpm 9 或更高版本

## 开发模式启动

在项目根目录执行：

```bash
pnpm install
pnpm db:migrate
pnpm dev
```

启动成功后，在浏览器访问：

```text
http://localhost:4321
```

## 生产模式启动

在项目根目录执行：

```bash
pnpm install --frozen-lockfile
pnpm db:migrate
pnpm build
pnpm start
```

启动成功后，在浏览器访问：

```text
http://localhost:3000
```

> 已经安装过依赖时，可以跳过依赖安装命令。

## 首次创建管理员

复制 `.env.example` 为 `.env.local`，将初始密码替换为至少 12 位的随机强密码，然后执行：

```bash
pnpm admin:init
```

初始化成功后，登录 `/admin/login` 验证账号，并从服务器环境中移除 `ADMIN_INITIAL_PASSWORD`。数据库中已有管理员时，重复执行不会覆盖原密码。

## 持久化目录

部署和服务器重启时必须保留：

- `data/blog.sqlite`、`data/blog.sqlite-wal` 和 `data/blog.sqlite-shm`
- `public/uploads/`

这些路径已被 Git 忽略。部署脚本只会创建目录，不会删除或覆盖数据库和上传文件。

## 备份与恢复

先创建一致的 SQLite 快照，再备份上传目录：

```bash
mkdir -p backups
sqlite3 data/blog.sqlite ".backup 'backups/blog.sqlite'"
tar -czf backups/uploads.tar.gz public/uploads
```

恢复前先停止博客服务，将备份数据库放回 `data/blog.sqlite`、解压上传目录，再启动 PM2。数据库与上传目录应作为同一批次一起备份。
