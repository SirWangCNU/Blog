# 项目启动教程

## 环境要求

- Node.js 20.9.0 或更高版本（自带 npm）

## 开发模式启动

在项目根目录执行：

```bash
npm install --package-lock=false --registry=https://registry.npmjs.org --replace-registry-host=always
npm run dev
```

启动成功后，在浏览器访问：

```text
http://localhost:4321
```

## 生产模式启动

在项目根目录执行：

```bash
npm install --package-lock=false --registry=https://registry.npmjs.org --replace-registry-host=always
npm run build
npm run start
```

启动成功后，在浏览器访问：

```text
http://localhost:3000
```

> 已经安装过依赖时，可以跳过依赖安装命令。
