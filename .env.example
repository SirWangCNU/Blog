# SQLite database location. Keep this path on persistent disk in production.
BLOG_DATABASE_PATH=data/blog.sqlite

# Used only by `pnpm admin:init` when the database has no administrator.
ADMIN_USERNAME=admin
ADMIN_INITIAL_PASSWORD=replace-with-a-random-password-of-at-least-12-characters
